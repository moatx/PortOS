learn about text mode cursors
multi-boot support
make docs (gdt)
get console running (partially done)
add VA to printf
serial driver for debugging
interrupt support
user space support
fix build.sh

BBB:
	arm boot-loader
	gpio driver


investigate:
	mbr boot-loader


