multi-boot support
make docs (gdt)
get console running (partially done)
build.sh remake + make it remove the build dir if an error occurred
interrupter support
user space support
custom core utils (to learn unix)

BBB:
	arm boot-loader
	gpio driver


investigate:
	sign boot-loader in asm 
	mbr boot-loader


