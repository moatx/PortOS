multi-boot support
make docs (gdt)
get console running (partially done)
add VA to printf
serial driver for debugging
interrupter support
try netbsd current lint
user space support
fix build.sh
custom core utils (to learn unix)

BBB:
	arm boot-loader
	gpio driver


investigate:
	sign boot-loader in asm 
	mbr boot-loader


